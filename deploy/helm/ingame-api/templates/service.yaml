apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingame-api.fullname" . }}
  labels:
    {{- include "ingame-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingame-api.selectorLabels" . | nindent 4 }}
